# Agent Definition Improvement Todos

## TODOs

- [ ] gamedev-agent: Add version, inputs, outputs, examples to frontmatter; clarify boundaries; add sample workflow and example request/response.
- [ ] test_agent: Add version, inputs, outputs, examples to frontmatter; add explicit “never do” and “ask first” lists; include sample workflow and example test run.
- [ ] devops-infra-agent: Fix YAML frontmatter list formatting; add version, inputs, outputs, examples; add explicit boundaries and escalation protocol; add sample workflow and example infra change.
- [ ] docs_agent: Fix YAML frontmatter list formatting; add version, inputs, outputs, examples; add explicit boundaries and escalation protocol; add sample workflow and example doc update.
- [ ] tracker_agent: Add version, inputs, outputs, examples to frontmatter; add explicit boundaries and escalation protocol; add sample workflow and example tracker update.
- [ ] game-design: Add version, inputs, outputs, examples to frontmatter; add explicit boundaries and escalation protocol; add sample workflow and example design change.
- [ ] execdocs-agent: Add version, inputs, outputs, examples to frontmatter; add explicit boundaries and escalation protocol; add sample workflow and example exec doc authoring.
- [ ] agent_author_agent: Add examples to frontmatter; add explicit boundaries and escalation protocol; add sample workflow and example agent authoring.
- [ ] git_agent: Add version, inputs, outputs, examples to frontmatter; add explicit boundaries and escalation protocol; add sample workflow and example merge operation.

---

## Progress Log

- [ ] gamedev-agent improvements (in progress)
